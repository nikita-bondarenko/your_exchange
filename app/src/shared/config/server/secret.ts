import { ProjectServerData } from "@/shared/model/project";

const API_URL = 'https://secret-backend.partners-bot.ru'; 
const USERNAME = "admin"
const PASSWORD = "GiY4TAaV4JdihmC"

export const SECRET_SERVER_DATA: ProjectServerData = {
    apiUrl: API_URL,
    username: USERNAME,
    password: PASSWORD
}